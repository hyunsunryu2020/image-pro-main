/**
 * Config: 用于表示配置对象的类型。例如：AppConfig, FetchConfig。
 *
 * ✔Params: 用于表示****函数****的参数类型。例如：getUserParams, searchParams。
 *
 * ✔(不变)State: 用于表示****状态对象****的类型。例如：CounterState, FormState。
 *
 * Event: 用于表示事件对象的类型。例如：MouseEvent, KeyboardEvent。
 *
 * ✔(变)Data: 用于表示****数据对象****的类型。例如：UserData, ProductData。
 *
 * ✔Options: 用于表示函数的可选参数或****配置选项****的类型。与之前提到的相同。
 *
 * ✔Props: 用于表示****组件的 props ****类型。与之前提到的相同。
 */